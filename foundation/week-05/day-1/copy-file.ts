// Write a function that copies the contents of a file into another
// It should take the filenames as parameters
// It should return a boolean that shows if the copy was successful

// let fs = require('fs');

// function copyFile (fileName_1: string, fileName_2: string) {
//     try {
//         let copyText = fs.readFileSync(fileName_1, 'utf8')
//         return fs.writeFileSync(fileName_2, copyText)
//     }

//     catch {

//         let error = new Error ('Copy was not successful')

//     }

// }


// console.log(copyFile('my-file.txt', 'writeme.txt'))

